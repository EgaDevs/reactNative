import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    form: {
        alignItems: 'center',
        rowGap: 15,
        width: 300,
        marginHorizontal: 'auto'
    },
    input: {
        width: "100%",
        height: 30,
        margin: "auto",
        borderColor: "#c8c8c8",
        borderWidth: 2,
        borderRadius: 5,
        marginBottom: 10,
        padding: 10,
    },
})